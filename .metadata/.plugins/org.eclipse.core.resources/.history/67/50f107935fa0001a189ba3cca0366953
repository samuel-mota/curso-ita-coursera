import java.text.SimpleDateFormat;
import java.util.Date;

public class Relogio {

	private long currentDate = System.currentTimeMillis();
	
	public Date agora() {
		//if (currentTime != 0) {
		Date date = new Date(TimeUnit.SECONDS.toMillis(currentDate);
		
			return date;
		//}
		//return 0;
	}
}